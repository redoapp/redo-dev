properties:
  orderedShippingMethodIds:
    description: >-
      Shipping method IDs in display order, first shown first. Must list every
      shipping method in the experience exactly once.
    items: { type: string }
    type: array
required: [orderedShippingMethodIds]
title: Shipping method order request
type: object
