description: Flag return.
post:
  description: Flag return
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema: { $ref: ../schema/return.schema.yaml }
  security:
    - Bearer: []
  tags: [Returns]
summary: Flag return
