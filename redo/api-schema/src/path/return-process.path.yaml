description: Process return.
post:
  description: Process return
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: ../schema/return.schema.yaml
  security:
    - Bearer: []
  tags:
    - Returns
summary: Process return
